---
name: self-review
description: コミットする前に、これからコミットする変更をセルフレビューする。「コミットして」に限らず、「〜を修正して PR を作って」「コミットしつつ PR して」のようにコミットを暗黙に含む依頼でも、コミット前に必ずこのスキルでレビューし、指摘があれば修正してからコミットする。明示的なセルフレビュー依頼にも使う。
context: fork
agent: Explore
---

# self-review

これからコミットする変更をセルフレビューし、結果を呼び出し元に報告する。

## 手順

### 1. レビュー観点を組み立てる

同梱の観点ファイルを読み込む（パスはこのスキルのディレクトリからの相対）。

- **常に** `references/common.md` を読む。
- プロジェクトの言語に対応する観点ファイルがあれば追加で読む（複数該当すれば全て加算）。

| 判定材料 | 追加で読む観点 |
|---|---|
| `Gemfile` または `*.gemspec` がある | `references/ruby.md` |

### 2. 変更をレビューする

`git status` と `git diff HEAD`（必要に応じて `git diff --staged`）で、最後のコミット以降の未コミット変更を確認する。**未コミットの変更が無ければ、その旨を報告して終了する。**

未コミットの変更があれば、次の順でレビューする。

1. **基礎レビュー**: 組み込みの `code-review` スキルを effort `medium` で実行し、その指摘を基礎レビュー結果として控える。`--fix` や `--comment` は付けない（修正の適用は呼び出し元に任せる）。
2. **プロジェクト規約の把握**: CLAUDE.md を読んでプロジェクトの規約と慣習を把握する。
3. **追加観点でのレビュー**: 手順 1（レビュー観点を組み立てる）で読み込んだ観点ファイルを、基礎レビューが拾いにくい領域（RSpec の規約、テストの価値判断、プロジェクト規約など）を中心に当てて追加チェックする。基礎レビューと重複する指摘は統合する。
4. **統合**: 基礎レビュー結果と追加チェックの指摘を、ファイル・行単位でまとめて 1 つの指摘リストにする。

git worktree で作業している場合（作業ディレクトリが `.claude/worktrees/` 配下）は、git コマンドは `cd <path> && git` ではなく `git -C <worktree-path>` を使う。また可能な限り `&&` でコマンドを連結せず、1 コマンドずつ実行する。

### 3. 呼び出し元に報告する

レビュー結果に応じて次を返す。

- **指摘がある場合**：指摘内容（と必要なら修正案）を返す。呼び出し元はそれを修正し、次にコミットしようとする際に改めてこのスキルでレビューする。
- **指摘がない場合**：`references/commit.md` の方針に従って、どうコミット・整理するのが良いか（未マージのコミットの整え方を含む）を伝える。呼び出し元はそれに沿ってコミットする。
